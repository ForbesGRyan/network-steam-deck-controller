# Cargo
/target/

# IDE / editor
/.idea/
/.vscode/
*.iml
*.swp

# Visual Studio (driver project)
/.vs/
/CppProperties.json
*.vcxproj.user
# Build output for the WDK project — outside Cargo's /target/ above.
/driver/NetworkDeckController/x64/
/driver/NetworkDeckController/ARM64/
# Auto-generated INF that VS drops next to the .vcxproj — we use the
# hand-authored one at driver/inf/NetworkDeckController.inf instead.
/driver/NetworkDeckController/NetworkDeckController.inf

# OS junk
Thumbs.db
.DS_Store

# Reference materials — GPL-2.0+ kernel sources, fetched on demand.
# See tools/README.md.
/tools/reference/

# Local environment overrides
.env
*.local

# Local hidraw captures (use with `client-win --replay`). Per-device
# binaries; no value in committing.
*.bin
