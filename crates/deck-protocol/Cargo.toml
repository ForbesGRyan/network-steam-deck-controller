[package]
name = "deck-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Steam Deck controller HID layout, canonical state, and network wire format"

[dependencies]
bitflags = "2"

[lints]
workspace = true
